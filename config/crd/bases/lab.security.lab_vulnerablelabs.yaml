---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: vulnerablelabs.lab.security.lab
spec:
  group: lab.security.lab
  names:
    kind: VulnerableLab
    listKind: VulnerableLabList
    plural: vulnerablelabs
    singular: vulnerablelab
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VulnerableLab is the Schema for the vulnerablelabs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VulnerableLabSpec defines the desired state of VulnerableLab.
            properties:
              vulnerability:
                enum:
                - K01
                - K02
                - K03
                - K06
                - K07
                - K08
                type: string
            type: object
          status:
            description: VulnerableLabStatus defines the observed state of VulnerableLab.
            properties:
              chosenVulnerability:
                description: The chosen vulnerability for this instance
                type: string
              message:
                description: A message for the student
                type: string
              state:
                description: State of the lab (Vulnerable, Remediated, Error)
                type: string
              targetResource:
                description: The specific target for the vulnerability (e.g., a deployment
                  name for K01)
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
